# Minimal dependencies for running app.py on the EC2 instance.
# Deliberately excludes onnxruntime/opencv/huggingface_hub/tqdm/numpy/Pillow --
# those are only needed for local tagging (batch_tag.py) and migration
# (migrate_to_aws.py), never on the server.
Flask==3.1.0
Flask-HTTPAuth==4.8.0
gunicorn==23.0.0
psycopg2-binary==2.9.10
boto3==1.35.99
